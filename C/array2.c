//In given integer array, check if it contains a subarray having zero-sum.
#include <stdio.h>

void subarray(int *arr, int n)
{
  int i=0;j=0;
  int sum=0;
  int trv=0;
  

}

int main()
{
  int n=0;
  scanf("%d",&n);
  int arr[n];
  for(int i=0;i<n;i++)
    scanf("%d",&arr[i]);

  subarray(arr, n);

  return 0;
}
